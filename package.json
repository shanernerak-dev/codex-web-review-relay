{
  "name": "single-crystal-review-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node --experimental-strip-types src/cli.ts serve",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "test:compat": "node --experimental-strip-types scripts/verify-compatibility.ts"
  }
}
